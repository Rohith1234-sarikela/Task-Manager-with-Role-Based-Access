# Task Manager Frontend

Frontend (React + Vite)

Setup

1. Install dependencies:

```
cd frontend
npm install
```

2. Run dev server:

```
npm run dev
```

Default API base is `http://localhost:5000/api`. Change `src/services/api.js` if needed.
